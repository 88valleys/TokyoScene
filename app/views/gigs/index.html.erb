<h1> All Gigs </h1>
<% @gigs.each do |gig| %>
  <%= gig.name %>
<% end %>
<%# PSEUDOCODE %>
<%# 1. Clicking on a marker updates the info on the bottom of th emap %>
<%# MAPBOX %>
<div data-controller="map" class="map-container" data-map-markers-value="<%= @markers.to_json %>" data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  <div class="mapbox" style="width: 100%; height: 600px;"
  data-map-target="mapElement"
  >
  </div>
  <div class="content-wrapper" data-map-target="gig">
  </div>
  <%= link_to "Back to Search", root_path, class: "btn btn-flat"%>
</div>